/**
 * It returns the current scroll position of the window.
 * @returns The scroll position of the window.
 */
export declare const useGetScrollPosition: (initialPosition?: Number) => number | Number;
